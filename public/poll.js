socket.on("clickVote", function (poll) {
  //grab all response elements and update span with new vote count
  // console.log('hello')
  // var clickVote = document.getElementById(vote);
  // clickVote.innerText = vote;
});
